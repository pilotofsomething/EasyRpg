package pilotofsomething.easyrpg.particle;

import net.minecraft.client.particle.NoRenderParticle;
import net.minecraft.client.world.ClientWorld;

public class NoShowParticle extends NoRenderParticle {
	public NoShowParticle(ClientWorld clientWorld, double d, double e, double f, double g, double h, double i) {
		super(clientWorld, d, e, f, g, h, i);
	}
}
